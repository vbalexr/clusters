apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tunnels
resources:
  - namespace.yaml

  - secrets-cloudflared.yaml
  - ../../../apps/cloudflared

  - npm-mariadb-secrets.yaml
  - ../../../apps/npm

patches:
  - path: patches/npm.yaml