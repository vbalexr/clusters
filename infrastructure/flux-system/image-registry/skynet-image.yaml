apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: skynet
  namespace: flux-system
spec:
  image: ghcr.io/mrakaki/sspti-skynet
  interval: 1m0s
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: skynet
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: skynet
  filterTags:
    pattern: "^20[0-9]{10}$"
    extract: "$0"
  policy:
    numerical:
      order: desc
